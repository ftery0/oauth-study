# OAuth 서버 정보
OAUTH_SERVER_URL=http://localhost:8080

# 이 앱의 클라이언트 자격증명 (OAuth 서버에 등록된 값과 일치해야 함)
OAUTH_CLIENT_ID=example-app
OAUTH_CLIENT_SECRET=secret

# OAuth 콜백 URI (OAuth 서버의 allowedClients에 등록된 값과 일치해야 함)
OAUTH_REDIRECT_URI=http://localhost:5173/callback

# 프론트엔드 주소 (로그인 완료 후 redirect 목적지)
FRONTEND_URL=http://localhost:5173

# 세션 암호화 키 (프로덕션에서는 충분히 길고 랜덤한 값으로 교체)
SESSION_SECRET=dev-session-secret-change-in-production
